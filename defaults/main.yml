---
refertest_db_name: 'refertest'
refertest_db_user: 'Refertest_user'
refertest_db_password: 'Refertest'

refertest_dir: '/opt/refertest'
refertest_user: 'refertest'
refertest_group: 'tomcat'

refertest_resources_dir: "{{ refertest_dir }}/resources"
refertest_tmp_dir: '/tmp/refertest'
refertest_tomcat_dir: '/opt/tomcat'
